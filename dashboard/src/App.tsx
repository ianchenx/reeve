/**
 * App.tsx — kept as a minimal shell for backwards compatibility.
 *
 * The actual routing and layout is handled by router.ts → RootLayout.tsx.
 * This file is no longer imported by main.tsx.
 */
export {}
